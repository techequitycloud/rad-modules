## 2024-07-11 - Correcting UIMeta Group Assignments
**Learning:** Variables often accidentally default to `group=0` during copy-paste, breaking wizard logical page placement.
**Action:** Verify that `group=N` matches the hundreds digit of `order=Nxx` and the logical section definition in `SKILLS.md`.
