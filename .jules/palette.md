## 2024-06-25 - [Missing Sensitive Flag] **Learning:** Sensitive data like external IPs for RDP connection and private IPs are exposed in Terraform outputs because they are not marked with `sensitive = true`. **Action:** Add `sensitive = true` to outputs containing passwords, credentials, or private/internal IPs to improve the operator experience and follow security best practices.
