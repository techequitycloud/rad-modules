## 2026-05-01 - Standard GKE Outputs **Learning:** Standard GKE-based modules must include a `cluster_credentials_cmd` output in `outputs.tf` to provide operators with the necessary `gcloud container clusters get-credentials` authentication command, as defined in `SKILLS.md`. I noticed `Bank_GKE` and `MC_Bank_GKE` are missing this output. **Action:** Next time I work on a GKE module, check that `cluster_credentials_cmd` is exposed in `outputs.tf`.
